#include <cstdio>
#include <cmath>

int main() {

    // Standard math functions are in the <cmath> header
    double a,b,c;
    a=sqrt(1.2);
    b=4*atan(1.0);
    c=tanh(5.0);

    // Formatted print
    printf("%g %g %g\n",a,b,c);
}
